channels:
  - conda-forge
  - default
dependencies:
  - astropy>=4.2
  - python=3.8 # nail the python version, so conda does not try upgrading / dowgrading
  - ctapipe=0.12
  - eventio
  - corsikaio
  - protozfits=2.0
  - zeromq
  - ipython
  - numba
  - numpy>=1.18
  - numpydoc
  - pytest
  - pyyaml
  - zlib
  - pip
  - h5py
  - pip:
      - pytest_runner
